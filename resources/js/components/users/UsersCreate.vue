<template>
    <div v-if="errors">
        <div v-for="(value, key) in errors" :key="key" class="bg-red-500 text-white rounded font-bold mb-4 shadow-lg py-2 px-4 pr-0">
            <p v-for="error in value" :key="error" class="text-sm">
                {{error}}
            </p>
        </div>
    </div>

    <form class="space-y-6 container" @submit.prevent="saveUser">
            <div class="space-y-4 rounded-md">
                <div>
                    <label for="name" class="block text-sm font-medium text-gray-700">Nume
                    </label>
                    <div class="col-sm-3 mt-1">
                        <input type="text" name="name" id="name"
                               class="block mt-1 w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300
                               focus:ring focus:ring-indigo-200 focus:ring-opacity-50"
                               v-model="form.name">
                    </div>
                </div>

                <div>
                    <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
                    <div class="col-sm-3 mt-1">
                        <input type="text" name="email" id="email"
                               class="block mt-1 w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300
                               focus:ring focus:ring-indigo-200 focus:ring-opacity-50"
                               v-model="form.email">
                    </div>
                </div>

                <div>
                    <label for="address" class="block text-sm font-medium text-gray-700">Parola</label>
                    <div class="col-sm-5 mt-1 inline-flex">
                        <input type="password" name="password" id="password"
                               class="block mt-1 w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-300
                               focus:ring focus:ring-indigo-200 focus:ring-opacity-50"
                               v-model="form.password">
<!--                        <button type="button" @click="generatePassword"-->
<!--                                class="left-2 inline-flex items-center px-4 py-2 text-xs font-semibold tracking-widest text-white uppercase-->
<!--                bg-gray-800 rounded-md border border-transparent ring-gray-300 transition duration-150 ease-in-out-->
<!--                hover:bg-gray-700 active:bg-gray-900 focus:outline-none focus:border-gray-900 focus:ring disabled:opacity-25">-->
<!--                            Generate password-->
<!--                        </button>-->
                    </div>

                </div>
            </div>

        <button type="submit"
                class="inline-flex items-center px-4 py-2 text-xs font-semibold tracking-widest text-white uppercase
                bg-gray-800 rounded-md border border-transparent ring-gray-300 transition duration-150 ease-in-out
                hover:bg-gray-700 active:bg-gray-900 focus:outline-none focus:border-gray-900 focus:ring disabled:opacity-25">
            Adauga
        </button>
    </form>
</template>

<script>
import { reactive } from "vue";
import useUsers from "../../composables/users";

export default {
    // password:'',
    setup() {
        const form = reactive({
            'name': '',
            'email': '',
            'password': '',
        })

        const {errors, storeUser} = useUsers()
x``
        const saveUser = async () => {
            await storeUser({...form});
        }

        return {
            form,
            errors,
            saveUser,
            password: ''
        }
    }
}
</script>
